import { WizardRequiredStepDirective } from './wizard-required-step.directive';

/* tslint:disable:no-unused-variable */

describe('Directive: WizardRequiredStepDirective.ts', () => {
  it('should create an instance', () => {
    const directive = new WizardRequiredStepDirective();
    expect(directive).toBeTruthy();
  });
});
